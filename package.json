{
  "name": "fitting",
  "version": "0.1.0",
  "private": true,
  "description": "핏팅 — 착용경험 기반 브랜드 사이즈 번역. 규칙 엔진(web/js/engine.js)이 정본.",
  "scripts": {
    "test": "node engine/test.js",
    "serve": "node scripts/serve.js",
    "screens": "python3 scripts/gen-screens.py",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "prettier": "3.9.4"
  }
}
